import { router,useLocalSearchParams } from 'expo-router';
import { useState } from 'react';
import { Pressable,SafeAreaView,StyleSheet,Text,TextInput,View } from 'react-native';
import { api } from '../../src/services/api/client';
export default function ResetPassword(){const params=useLocalSearchParams<{token?:string}>();const[token,setToken]=useState(params.token??'');const[password,setPassword]=useState('');const[error,setError]=useState('');const submit=async()=>{setError('');try{await api('/v1/auth/reset-password',{method:'POST',body:JSON.stringify({token,password})});router.replace('/(auth)/login')}catch(e){setError(e instanceof Error?e.message:'Reset failed')}};return <SafeAreaView style={s.page}><View style={s.card}><Text style={s.title}>Choose a new password</Text>{!params.token&&<TextInput autoCapitalize="none" value={token} onChangeText={setToken} placeholder="Reset token" style={s.input}/>}<TextInput secureTextEntry value={password} onChangeText={setPassword} placeholder="New password (12+ characters)" style={s.input}/>{error?<Text style={s.error}>{error}</Text>:null}<Pressable disabled={!token||password.length<12} onPress={()=>void submit()} style={s.button}><Text style={s.buttonText}>Update password</Text></Pressable></View></SafeAreaView>}
const s=StyleSheet.create({page:{flex:1,justifyContent:'center',padding:24,backgroundColor:'#F4F7F5'},card:{gap:16},title:{fontSize:30,fontWeight:'800',color:'#13231D'},input:{backgroundColor:'#fff',borderRadius:14,padding:16},button:{backgroundColor:'#16805B',padding:16,borderRadius:14,alignItems:'center'},buttonText:{color:'#fff',fontWeight:'700'},error:{color:'#B42318'}});
