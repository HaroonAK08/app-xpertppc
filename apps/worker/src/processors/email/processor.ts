import type { Job } from 'bullmq';
type EmailJob={to:string;template:'PASSWORD_RESET'|'ORGANIZATION_INVITATION';variables:Record<string,string>};
export class EmailProcessor{constructor(private readonly deliveryUrl:string|undefined,private readonly token:string|undefined,private readonly from:string|undefined){}async process(job:Job<EmailJob>){if(!this.deliveryUrl||!this.token||!this.from)throw new Error('Transactional email provider is not configured');const response=await fetch(this.deliveryUrl,{method:'POST',headers:{authorization:`Bearer ${this.token}`,'content-type':'application/json'},body:JSON.stringify({from:this.from,to:job.data.to,template:job.data.template,variables:job.data.variables})});if(!response.ok)throw new Error(`Transactional email delivery failed (${response.status})`);return{delivered:true}}}
