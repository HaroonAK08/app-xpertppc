import { CanActivate,ExecutionContext,ForbiddenException,Injectable } from '@nestjs/common';import type { AuthenticatedRequest } from '../common/auth-context.js';import { DatabaseService } from '../common/database.module.js';@Injectable()export class SystemAdminGuard implements CanActivate{constructor(private readonly db:DatabaseService){}async canActivate(context:ExecutionContext){const auth=context.switchToHttp().getRequest<AuthenticatedRequest>().auth;const user=await this.db.user.findUnique({where:{id:auth.userId},select:{isSystemAdmin:true}});if(!user?.isSystemAdmin)throw new ForbiddenException('System administrator access is required.');return true}}
